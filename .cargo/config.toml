# Cargo 配置文件

# 网络配置
[net]
retry = 5
git-fetch-with-cli = true

# 编译配置
[build]
# 使用所有 CPU 核心
jobs = 8

# 如果官方源无法访问，可以尝试以下镜像源（取消注释）：
# [source.crates-io]
# replace-with = "rsproxy"
#
# [source.rsproxy]
# registry = "https://rsproxy.cn/crates.io-index"
